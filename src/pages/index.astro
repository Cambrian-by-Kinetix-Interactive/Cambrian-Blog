---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { BLOG_CATEGORIES, SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const categories = BLOG_CATEGORIES.slice(0, 4);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.hero {
				max-width: var(--max-width-wide);
				margin: 0 auto;
				padding: 6em 1.5em 4em;
				text-align: center;
			}
			.hero-label {
				font-size: 0.75rem;
				font-weight: 600;
				text-transform: uppercase;
				letter-spacing: 0.1em;
				color: var(--label-color);
				margin-bottom: 1.5em;
			}
			.hero h1 {
				font-size: 3em;
				letter-spacing: -0.03em;
				margin-bottom: 0.5em;
				color: var(--text-primary);
			}
			.hero p {
				font-size: 1.125rem;
				color: var(--text-secondary);
				max-width: 560px;
				margin: 0 auto 1.5em;
				line-height: 1.6;
			}
			.category-pills {
				display: flex;
				flex-wrap: wrap;
				justify-content: center;
				gap: 0.75rem;
				list-style: none;
				margin: 1.5rem 0 0;
				padding: 0;
			}
			.category-pills a {
				display: inline-flex;
				padding: 0.55rem 0.9rem;
				border: 1px solid var(--border-color);
				border-radius: 999px;
				background: var(--bg-card);
				font-size: 0.8125rem;
				color: var(--text-secondary);
				text-decoration: none;
				transition: all var(--transition);
			}
			.category-pills a:hover {
				border-color: var(--accent);
				color: var(--text-primary);
			}
			.hero .hero-subtitle {
				font-size: 1.25rem;
				color: var(--text-secondary);
				font-weight: 500;
				margin-bottom: 1em;
			}
			.hero-cta {
				display: inline-flex;
				align-items: center;
				gap: 0.5em;
				padding: 0.75em 1.75em;
				background: var(--accent);
				color: var(--bg-primary);
				border-radius: 24px;
				font-weight: 600;
				font-size: 0.9375rem;
				transition: all var(--transition);
			}
			.hero-cta:hover {
				background: var(--accent-hover);
				color: var(--bg-primary);
			}
			@media (max-width: 720px) {
				.hero {
					padding: 4em 1em 3em;
				}
				.hero h1 {
					font-size: 2em;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<section class="hero">
			<h1>The AI Music Marketplace Blog</h1>
			<p class="hero-subtitle">Insights on AI music, licensing, and building in the new music economy.</p>
			<p>We write about what's happening in AI music — how to create it, license it, sell it, and buy it. Whether you're a creator building a catalog or a buyer looking for commercial-ready tracks, this is where we share what we know.</p>
			<a href="/blog" class="hero-cta">Read the Blog</a>
			<ul class="category-pills">
				{
					categories.map((category) => (
						<li><a href={`/blog/category/${category.slug}/`}>{category.name}</a></li>
					))
				}
			</ul>
		</section>
		<Footer />
	</body>
</html>
